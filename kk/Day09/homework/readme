1. 知识整理
2. socket
    fileserver
        ls/cat
        upload/download/delete

        => 文件远程复制  server
                    client -local xxx -remote xxxx
    chats
        => point2point
        => 群聊
            Read 带缓冲的 阻塞

            conn.SetDeadline
                    setReadDeadline
                    setWirteDeadline
                    只有一次有效，需要重新配置

    代理 nginx 反向代理
        proxy  -addr -host:xxxx
            server

        client 连接到server后，将所有流量代理到指定的host上

3. http
    todolist
        add 添加   post
        query 查询 get
        delete 删  post
        modify 改  post